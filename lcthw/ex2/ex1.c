#include <stdio.h>

// Compile this by and with `gcc ex1.c`, then run it with `./a.out`.

int main(int args, char *argv[])
{
  int distance = 100;
  printf("You are %d miles away.\n", distance);
  return 0;
}
